/** @format */

import customConfig from './normal.config';

export default defineAppConfig({
  ...customConfig,
  window: {
    backgroundTextStyle: 'light',
    navigationBarBackgroundColor: '#0B4F4A',
    navigationBarTextStyle: 'white',
  },
});
